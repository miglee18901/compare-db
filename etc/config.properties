# 1: compare record details; 2: compare record counts only.
MODE=1

# Number of records loaded per query batch.
BATCH_SIZE=1000

# Output directory, relative to the repository working directory.
PATH_STATISTICS_FILE=result
